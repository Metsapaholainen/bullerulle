FMP_API_KEY=your_financial_modeling_prep_api_key_here
NTFY_TOPIC=your_private_ntfy_sh_topic_name_here
# Fine-grained GitHub PAT, scoped to just this repo with Contents: read/write --
# only needed so Paper Trading / Sell Alerts / Watchlist data persists across
# restarts (see the "Persistent data" section in DEPLOY.md). Safe to leave
# unset locally -- everything falls back to plain local files without it.
GITHUB_TOKEN=your_fine_grained_github_pat_here
